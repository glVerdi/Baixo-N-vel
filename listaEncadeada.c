typedef struct {
    Node* head;
    Node* tail;
    int size;   // conveniente sempre saber o tamanho
} List;

len(List lst)
add(List* lst, int value)
dump(List lst)
create(List* lst) // List* create ()
